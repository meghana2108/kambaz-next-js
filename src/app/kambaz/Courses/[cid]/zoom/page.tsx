export default function Zoom() {
    return (
        <div>
            <h1>Zoom</h1>
            <p>This is a static page which lets us to do changes to zoom section here. We can upload zoom recordings over here.</p>
        </div>
    );
}
